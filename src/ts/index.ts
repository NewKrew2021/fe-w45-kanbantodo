import './Task'
import './Note'
import './Column'
import './Kanban'
import Kanban from './Kanban'

const kanban = new Kanban({ id: 'kanban' })

window.addEventListener('DOMContentLoaded', (event) => {
  
})